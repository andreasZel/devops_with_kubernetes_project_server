apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: project
  name: shared-todos-pv-claim 
spec:
  storageClassName: shared-todos-pv 
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi